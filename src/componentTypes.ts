export type MarkdownInputProps = {
  testID?: string;
  style?: FixMe;
};

export type ToolbarProps = {
  testID: string;
  isFocused?: boolean;
};

export type ToolbarItemProps = {
  controlName: string;
  testID: string;
};
